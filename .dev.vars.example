# 本地开发用的环境变量（拷贝为 .dev.vars 后填值，.dev.vars 不会进 git）
# 生产部署用 `wrangler secret put JWT_SECRET` 设置同名 secret。

# JWT 签名密钥，32 字节随机字符串建议用：
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=
